name: traceway
description: Traceway error tracking and screen recording SDK for Flutter.
version: 0.1.2
homepage: https://tracewayapp.com
repository: https://github.com/tracewayapp/traceway-flutter

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  uuid: ^4.0.0
  path_provider: ^2.1.0
  flutter_quick_video_encoder: ^1.7.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
